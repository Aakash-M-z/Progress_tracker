import React from 'react';

const ResourcesPage = (props: any) => {
  return (
    <div className="resourcespage-container w-full h-full">
      {props.children}
    </div>
  );
};
export default ResourcesPage;
