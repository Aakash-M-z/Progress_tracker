import React from 'react';

const AnalyticsPage = (props: any) => {
  return (
    <div className="analyticspage-container w-full h-full">
      {props.children}
    </div>
  );
};
export default AnalyticsPage;
