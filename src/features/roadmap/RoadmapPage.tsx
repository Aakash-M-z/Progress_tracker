import React from 'react';

const RoadmapPage = (props: any) => {
  return (
    <div className="roadmappage-container w-full h-full">
      {props.children}
    </div>
  );
};
export default RoadmapPage;
